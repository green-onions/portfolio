{{ form_start(form) }}
<div class="margin-18">
    <p class="mb-2 mt-2 color-grey-dark">{{ form.name.vars.label }}</p>
    {{ form_widget(form.name) }}
    <p class="mb-2 mt-2 color-grey-dark">{{ form.imageFile.vars.label }}</p>
    {{ form_widget(form.imageFile) }}
    <p class="mb-2 mt-2 color-grey-dark">{{ form.link.vars.label }}</p>
    {{ form_widget(form.link) }}
</div>
<button class="btn check-btn-other float-right text-white txt-shadow mt-4">
    <i class="fa fa-check"></i>
</button>
{{ form_end(form) }}
