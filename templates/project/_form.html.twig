{{ form_start(form) }}
<div class="row">
    <div class="col-6 form-user-column">
        <p class="mb-2">{{ form.Title.vars.label }}</p>
        {{ form_widget(form.Title) }}
        <p class="mb-2 mt-2">{{ form.link.vars.label }}</p>
        {{ form_widget(form.link) }}
        <p class="mb-2 mt-2">{{ form.imageFile.vars.label }}</p>
        {{ form_widget(form.imageFile) }}
        <p class="mb-2 mt-2">{{ form.client.vars.label }}</p>
        {{ form_widget(form.client) }}
        <p class="mb-2 mt-2">{{ form.languages.vars.label }}</p>
        {{ form_widget(form.languages) }}
    </div>
    <div class="col-6 form-user-column">
        <p class="mb-2">{{ form.description.vars.label }}</p>
        {{ form_widget(form.description) }}
    </div>
</div>
<button class="btn check-btn float-right text-white txt-shadow mt-3">
    <i class="fa fa-check"></i>
</button>
{{ form_end(form) }}
