{% extends 'base.html.twig' %}

{% block title %}dev-apps\éditer-techno{% endblock %}

{% block body %}
    <div class="d-flex justify-content-center align-items-center surcontainer">
        <div class="form-other-container rounded shadow-sm">
            <h1 class="main-title text-center txt-shadow">Éditer la techno</h1>
            {{ include('language/_form.html.twig') }}
            {{ include('language/_delete_form.html.twig') }}
        </div>
    </div>
{% endblock %}
